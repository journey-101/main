# feat: add user preference recommendation api (#23)

## 관련 이슈

- #23

## 변경 사항

- 사용자 여행 성향 저장·조회 API를 추가했습니다.
- `user_preferences` 테이블과 개발용 기본 성향 seed를 추가했습니다.
- 독립 `recommendations` 도메인과 공통·편의 추천 endpoint를 추가했습니다.
- trip 추천의 W2 place fallback, 목업 점수 계산, API/DB 문서와 payload 예시를 추가했습니다.

## 배경

- W2에서 사용자 성향 기반 추천의 최소 동작을 검증해야 합니다.
- 기존 `plan-runs` 명칭은 실제 수행 시도 기록인 `trip_attempts`와 혼동될 수 있어 사용하지 않습니다.
- W3의 `trips.place_id` 도입 전에도 trip 요청 흐름을 열어 두기 위해, W2는 place 추천 fallback으로 처리합니다.

## 구현 상세

- `GET /api/v1/me/preferences`, `PUT /api/v1/me/preferences`를 구현했습니다.
- `POST /api/v1/recommendations`에서 `preference_mock` method와 `place`·`trip` target을 지원합니다.
- `/recommendations/places`, `/recommendations/trips`는 공통 request로 normalize하여 동일 service와 scorer를 사용합니다.
- trip target은 사용자·trip 소유권을 확인한 뒤 `trip_place_not_available` fallback과 `{ "type": "place" }` fallback target을 반환합니다.
- 추천 결과는 저장하지 않으며, 선호/회피 category, 조용함, 도보 제한, 첫 여행자 조건으로 점수와 reason을 계산합니다.
- 점수 내림차순, 장소명·place ID 오름차순으로 안정적으로 정렬합니다.
- Places API 공개 응답은 변경하지 않고 추천 metadata를 recommendations 도메인 내부에 두었습니다.

## 체크리스트

- [x] 로컬에서 정상 동작 확인
- [x] 테스트 코드 추가/수정
- [x] 기존 trip/place 테스트 통과 확인
- [x] 문서 업데이트
- [x] `uv run pytest tests -q` 통과 (46 passed)
- [x] `ruff check .` 및 `git diff --check` 통과
- [x] Python HTTP 요청으로 API 응답 확인
- [x] DB 초기화 컨테이너와 seed 데이터 확인
- [x] 불필요한 코드/주석 제거

Closes #23
